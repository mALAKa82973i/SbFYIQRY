  rubocop:
  bundler-audit:
    enabled: true
    enabled: true

ratings:
  - "**.rb"
# exclude_paths:
# - app/controllers/*.py # Exclude specific files type within a folder/subfolders
exclude_paths:
- spec/*
- docs/*